define(function(require) {
	var Nvm=require('nvm');
	var ndq=require('ndq');
	var events=require('events');
	
	var viewModel= {
				a:'aa'
			};

			
			};
			
			var nvm=new Nvm(document.getElementById("dic"));
			
			nvm.bindModel(viewModel);
	
}); 